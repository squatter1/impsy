# Define the stages of our pipeline
stages:
  - build

# LaTeX compilation job with BibTeX support
build_pdf:
  stage: build
  # Use a Docker image with TeX Live installed
  image: texlive/texlive:latest
  # Define what the job should do
  script:
    # Make sure we're in the right directory
    - cd $CI_PROJECT_DIR
    # Install any additional LaTeX packages if needed
    # - tlmgr install <package-name>
    # First LaTeX pass (creates .aux file for BibTeX)
    - pdflatex -interaction=nonstopmode mainfile.tex
    # Run BibTeX to process the bibliography
    - bibtex mainfile
    # Second LaTeX pass (incorporates bibliography)
    - pdflatex -interaction=nonstopmode mainfile.tex
    # Final LaTeX pass (resolves all references)
    - pdflatex -interaction=nonstopmode mainfile.tex
  # Define artifacts to be saved
  artifacts:
    paths:
      - mainfile.pdf
    expire_in: 1 week